# finance V0.7 Bug Scan Report (2026-05-15)

> 来源：codex 重度静态扫描（12 分钟 / 29 文件）+ 老板实测反馈。
> 配套工单见 TODO.md "P0 — 2026-05-15 重度扫描 bug 围剿" 段。

## 摘要

- P0 (阻塞业务/数据错): 5 个
- P1 (体验/视觉错): 8 个
- P2 (代码 smell): 2 个
- 合计 15 个

权限边界（D 类）扫描 0 bug — OWNER-only 页面有 page-guard + Server Action 双层兜底。

---

## G. 老板实测（最高优先级）

### bug-G-001: 审核详情面板的"通过/驳回"按钮被挤出可视区，老板点不到 [P0]
- 位置: src/app/(app)/approvals/approval-dialog.tsx:269（操作按钮区）
- 现象: 老板打开审核详情对话框后，"通过/驳回"按钮在内容最底部，需向下滚动才可见；老板报告"按钮在右下角点不到"，审核流程实际不可用。
- 真因（推测）: DialogContent 内容较长（含 max-h-480px 凭证大图），操作按钮区无 sticky 吸底，被推到滚动区底部；面板宽度 sm:max-w-4xl 在窄屏可能溢出。
- 修复建议: 操作按钮区固定吸底（sticky bottom）始终可见 + 面板宽度限制在视口内 + 内容区可滚动。

---

## A. 时间字段一致性

### bug-A-001: 共享时间格式化未固定 Asia/Shanghai，导致 Server/Client 显示可差 8 小时 [P0]
- 位置: src/lib/format.ts:10
- 现象: `formatDate` 使用 `Intl.DateTimeFormat("zh-CN")` 但未设置 `timeZone`，同一 `Date` 在服务端默认 UTC、客户端 Asia/Shanghai 时会显示不同小时。
- 真因（推测）: 时间展示依赖运行环境默认时区，而不是产品要求的 `Asia/Shanghai`。
- 修复建议: 在共享 formatter 中显式设置 `timeZone: "Asia/Shanghai"`，并让所有页面统一使用它。

### bug-A-002: 审核列表时间在 Server Component 渲染，详情弹窗时间在 Client Component 渲染 [P0]
- 位置: src/app/(app)/approvals/page.tsx:167
- 现象: 审核列表第一列用服务端格式化 `entry.createdAt`，详情弹窗用客户端格式化同一字段，运行时区不一致时会出现 `11:17` vs `19:17`。
- 真因（推测）: Server/Client 都调用未固定时区的 `formatDate`。
- 修复建议: 修复 `formatDate` 的 `timeZone`，或在服务端预格式化为稳定字符串再传给客户端。

### bug-A-003: 日期输入转 Date 使用服务端本地时区，不按 Asia/Shanghai 解释业务日期 [P1]
- 位置: src/server/entries.ts:30
- 现象: `YYYY-MM-DD` 被转换为 `new Date(...T00:00:00.000)`，结果依赖 Node 进程时区。
- 真因（推测）: 表单日期没有带时区，服务端本地时区被隐式使用。
- 修复建议: 将录入日期明确解释为上海时区日期边界，转换为对应 UTC instant 后入库。

### bug-A-004: 分配方案生效日期转 Date 使用服务端本地时区 [P1]
- 位置: src/server/allocations.ts:37
- 现象: `effectiveFrom` 的 `YYYY-MM-DD` 转换依赖 Node 进程时区，可能影响"当前生效方案"判断。
- 真因（推测）: 日期-only 字段没有显式上海时区语义。
- 修复建议: 和流水日期一样统一按 `Asia/Shanghai` 的 00:00 解释。

### bug-A-005: 用户管理页自定义 formatDate 未复用共享格式且未固定时区 [P2]
- 位置: src/app/(app)/admin/users/page.tsx:20
- 现象: 用户创建时间用局部 `toLocaleString("zh-CN")`，格式和其他页面的 `formatDate` 不一致，且无 `timeZone`。
- 真因（推测）: 页面内重复实现时间格式化。
- 修复建议: 删除局部 formatter，统一使用 `src/lib/format.ts` 中固定上海时区的函数。

## B. 金额字段一致性

### bug-B-001: 流水审计详情标题金额未按类型加负号 [P1]
- 位置: src/app/(app)/projects/[id]/[entryId]/page.tsx:77
- 现象: 审计详情头部直接 `formatYuan(entry.amountCents)`，支出流水会显示为正数，而项目列表/审核列表显示为负数。
- 真因（推测）: 详情页没有复用列表里的 `signedAmount` 规则。
- 修复建议: 用统一的金额符号函数处理 `EXPENSE`/`PROXY_PAY` 等展示金额。

### bug-B-002: 项目详情盈亏汇总与项目列表各自实现金额分类逻辑 [P2]
- 位置: src/app/(app)/projects/[id]/page.tsx:89
- 现象: 项目详情用两次 aggregate 分别统计 `INCOME` 和 `[EXPENSE, PROXY_PAY]`，项目列表用 `signedAmountCents` 逐条计算。
- 真因（推测）: 金额分类规则分散，未来新增类型时容易出现列表/详情不一致。
- 修复建议: 抽出统一的"是否计入盈亏/符号"函数，列表、详情、个人页、审核页共用。

## C. 表单输入边界

### bug-C-001: 流水金额没有最大值限制，极大金额会溢出 Prisma Int 或丢精度 [P0]
- 位置: src/server/entries.ts:16
- 现象: 金额只校验正数和两位小数，没有上限；`Number(value) * 100` 后可能超过安全整数或 Prisma `Int` 范围。
- 真因（推测）: 表单边界未覆盖极大值。
- 修复建议: 客户端和 Server Action 同时限制最大金额分，转换前检查 `Number.isSafeInteger` 和 Prisma Int 范围。

### bug-C-002: 添加用户密码只要求 6 位，弱密码未被拦截 [P1]
- 位置: src/server/admin-users.ts:21
- 现象: `123456` 这类弱密码满足 `min(6)` 并可创建用户。
- 真因（推测）: 密码强度规则只有长度下限。
- 修复建议: 增加复杂度校验，至少拦截常见弱密码和纯数字短密码。

## D. 权限边界

未发现 bug。

补充说明: OWNER-only 页面靠 page-level guard（approvals/page.tsx:53、allocations/new/page.tsx:19、admin/users/page.tsx:37）；Server Action mutate 函数也有角色兜底（entries.ts:345、allocations.ts:100、admin-users.ts:39）。middleware.ts:11 只做登录拦截，不做角色授权。

## E. 列表-详情数据一致性

### bug-E-001: 反向冲销新建的 reversal entry 没有自己的 LedgerEvent 时间线 [P0]
- 位置: src/server/entries.ts:558
- 现象: 冲销时只给原流水写 `ENTRY_REVERSED`，新建的反向流水没有 `ENTRY_CREATED` 或关联事件。
- 真因（推测）: 一次冲销产生两个 Entry 状态变化，但审计事件只绑定到原 Entry。
- 修复建议: 同一事务中为 reversal entry 也写入可查询的 LedgerEvent。

### bug-E-002: Entry 审计时间线只按当前 entryId 查询，无法显示分配方案事件 [P1]
- 位置: src/app/(app)/projects/[id]/[entryId]/page.tsx:59
- 现象: `ALLOCATION_PLAN_CREATED` 事件写入时 `entryId` 为空，因此单条流水时间线永远查不到该类事件。
- 真因（推测）: LedgerEvent 同时承载 Entry 事件和项目级事件，但查询模型只有 `entryId`。
- 修复建议: 为 LedgerEvent 增加 `projectId` 或拆分项目级时间线。

### bug-E-003: LedgerEvent 未覆盖项目创建和用户创建/编辑写操作 [P1]
- 位置: src/server/projects.ts:53
- 现象: `Project.create` 没有写 LedgerEvent，用户创建/编辑也没有写事件，和"覆盖所有写操作"的要求不一致。
- 真因（推测）: 审计日志只覆盖了 Entry 和 AllocationPlan 的部分写操作。
- 修复建议: 为项目、用户管理等 mutate 操作补充事件类型和 payload。

### bug-E-004: 个人页项目卡与趋势曲线对"我应得"的分配口径不一致 [P1]
- 位置: src/app/(app)/me/page.tsx:319
- 现象: 项目卡用当前最新生效份额乘以全量历史净利润，趋势曲线按流水发生时查找当时方案计算，两者数字对不上。
- 真因（推测）: 当前应得和历史累计应得使用了不同分配规则。
- 修复建议: 明确口径后统一计算；若按历史方案归属，应按每笔流水的 delta earnings 累加。

## F. 其他发现

### bug-F-001: evidence URL 工具生成 base64 key，但 API route 期望原始 uploads/evidences 路径 [P1]
- 位置: src/lib/evidence-url.ts:2
- 现象: `getEvidencePublicUrl` 把 r2Key base64 后拼到 `/api/evidence/`，但 API 的 `assertEvidenceKey` 要求解码后以 `uploads/evidences/` 开头。
- 真因（推测）: URL 编码策略和 API 参数校验策略不一致。
- 修复建议: 删除 base64 编码，统一使用 `encodeURIComponent(r2Key)`，或让 API route 显式解 base64。

---

## 扫描元信息
- codex 扫描耗时: 12 分钟，读 29 个文件
- TypeScript 编译: 0 errors
- bug-G-001 来源: 老板 2026-05-15 浏览器实测
